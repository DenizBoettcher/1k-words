# ── Dependencies & build ──────────────────────────────────────────────
node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# ── Secrets & environment (templates .example are committed) ─────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Prisma ────────────────────────────────────────────────────────────
# Ignore local SQLite dev DBs, but DO commit prisma/migrations/**.
prisma/*.db
prisma/*.db-journal
prisma/*.sqlite
node_modules/.prisma/
.prisma/

# ── Logs & OS ─────────────────────────────────────────────────────────
*.log
npm-debug.log*
*.tmp
.DS_Store
Thumbs.db
