# ── Dependencies & build ──────────────────────────────────────────────
node_modules/
dist/
.wrangler/
*.tsbuildinfo

# ── Generated Prisma client (recreated by `prisma generate`) ─────────
/src/generated/

# ── Secrets & environment (templates .example are committed) ─────────
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# ── Personal Cloudflare config (copy from wrangler.jsonc.example) ─────
wrangler.jsonc
!wrangler.jsonc.example

# ── Local D1 / SQLite ─────────────────────────────────────────────────
*.sqlite
*.db

# ── Logs & OS ─────────────────────────────────────────────────────────
*.log
npm-debug.log*
.DS_Store
